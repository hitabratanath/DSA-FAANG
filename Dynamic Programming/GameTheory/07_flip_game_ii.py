"""FLIP GAME II (LeetCode 294)

You are playing a Flip Game with your friend. You are given a string currentState 
that contains only '+' and '-'. You and your friend take turns to flip two consecutive 
"++" into "--". The game ends when a player can no longer make a move. 
The player who cannot make a move loses.

Return true if the starting player can guarantee a win.

Example 1:
Input: currentState = "++++"
Output: True
Explanation: Starting player flips middle "++++" -> "+--+". 
Opponent has no valid move.

Example 2:
Input: currentState = "+"
Output: False

Constraints:
- 1 <= currentState.length <= 60
- currentState[i] is either '+' or '-'
"""

def can_win(currentState):
    pass
